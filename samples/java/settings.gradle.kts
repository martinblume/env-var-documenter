rootProject.name = "env-var-sample-java"

pluginManagement {
    repositories {
        mavenLocal()           // look here first — locally published plugin
        gradlePluginPortal()
        mavenCentral()
    }
}
