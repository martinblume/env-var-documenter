rootProject.name = "env-var-sample"

pluginManagement {
    repositories {
        mavenLocal()           // look here first — locally published plugin
        gradlePluginPortal()
        mavenCentral()
    }
}
